interface:
  display_name: "Clinical Platform Safety Review"
  short_description: "Review clinical safety boundaries"
  default_prompt: "Use $clinical-platform-safety-review to review this change."
policy:
  allow_implicit_invocation: true
